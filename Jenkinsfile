pipeline {
    stages {
        stage {
            steps {
                echo 'Hello, Maven'
                sh 'mvn --version'
            }
        }
        stage {
            steps {
                echo 'Hello, JDK'
                sh 'java -version'
            }
        }
    }
}
